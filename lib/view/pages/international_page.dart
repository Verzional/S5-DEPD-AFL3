part of 'pages.dart';

class InternationalPage extends StatelessWidget {
  const InternationalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('International Shipping')),
      body: const Center(
        child: Text('International Shipping Page - Coming Soon'),
      ),
    );
  }
}
